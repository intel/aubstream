#
# Copyright (C) 2022-2025 Intel Corporation
#
# SPDX-License-Identifier: MIT
#

SET_FLAGS_FOR("GEN12LP" "TGLLP" "ADLS" "ADLP" "DG1" "ADLN")
SET_FLAGS_FOR("XE_HPG_CORE" "DG2" "MTL")
SET_FLAGS_FOR("XE_HPC_CORE" "PVC")
SET_FLAGS_FOR("XE2_HPG_CORE" "BMG" "LNL")

if(CMAKE_SIZEOF_VOID_P EQUAL 8)
  SET_FLAGS_FOR("XE3_CORE" "PTL")
  SET_FLAGS_FOR("XE3P_CORE" "CRI")
endif()

if(SUPPORT_ARL)
  ENABLE_ADDITIONAL_SKU("XE_HPG_CORE" "MTL")
  if(TESTS_ARL)
    TEST_ADDITIONAL_SKU("XE_HPG_CORE" "MTL")
  endif()
endif()

if(SUPPORT_NVLS)
  ENABLE_ADDITIONAL_SKU("XE3_CORE" "PTL")
  if(TESTS_NVLS)
    TEST_ADDITIONAL_SKU("XE3_CORE" "PTL")
  endif()
endif()

# Add supported and tested platforms
if(AUBSTREAM_SUPPORT_GEN12LP)
  if(AUBSTREAM_SUPPORT_TGLLP)
    ADD_PLATFORM_FOR_CORE_TYPE("SUPPORTED" "GEN12LP" "TGLLP")
    if(AUBSTREAM_TESTS_TGLLP)
      ADD_PLATFORM_FOR_CORE_TYPE("TESTED" "GEN12LP" "TGLLP")
    endif()
  endif()

  if(AUBSTREAM_SUPPORT_DG1)
    ADD_PLATFORM_FOR_CORE_TYPE("SUPPORTED" "GEN12LP" "DG1")
    if(AUBSTREAM_TESTS_DG1)
      ADD_PLATFORM_FOR_CORE_TYPE("TESTED" "GEN12LP" "DG1")
    endif()
  endif()

  if(AUBSTREAM_SUPPORT_ADLS)
    ADD_PLATFORM_FOR_CORE_TYPE("SUPPORTED" "GEN12LP" "ADLS")
    if(AUBSTREAM_TESTS_ADLS)
      ADD_PLATFORM_FOR_CORE_TYPE("TESTED" "GEN12LP" "ADLS")
    endif()
  endif()

  if(AUBSTREAM_SUPPORT_ADLP)
    ADD_PLATFORM_FOR_CORE_TYPE("SUPPORTED" "GEN12LP" "ADLP")
    if(AUBSTREAM_TESTS_ADLP)
      ADD_PLATFORM_FOR_CORE_TYPE("TESTED" "GEN12LP" "ADLP")
    endif()
  endif()

  if(AUBSTREAM_SUPPORT_ADLN)
    ADD_PLATFORM_FOR_CORE_TYPE("SUPPORTED" "GEN12LP" "ADLN")
    if(AUBSTREAM_TESTS_ADLN)
      ADD_PLATFORM_FOR_CORE_TYPE("TESTED" "GEN12LP" "ADLN")
    endif()
  endif()
endif()

if(AUBSTREAM_SUPPORT_XE_HPG_CORE)
  if(AUBSTREAM_SUPPORT_DG2)
    ADD_PLATFORM_FOR_CORE_TYPE("SUPPORTED" "XE_HPG_CORE" "DG2")
    if(AUBSTREAM_TESTS_DG2)
      ADD_PLATFORM_FOR_CORE_TYPE("TESTED" "XE_HPG_CORE" "DG2")
    endif()
  endif()

  if(AUBSTREAM_SUPPORT_MTL)
    ADD_PLATFORM_FOR_CORE_TYPE("SUPPORTED" "XE_HPG_CORE" "MTL")
    if(AUBSTREAM_TESTS_MTL)
      ADD_PLATFORM_FOR_CORE_TYPE("TESTED" "XE_HPG_CORE" "MTL")
    endif()
  endif()
endif()

if(AUBSTREAM_SUPPORT_XE_HPC_CORE)
  if(AUBSTREAM_SUPPORT_PVC)
    ADD_PLATFORM_FOR_CORE_TYPE("SUPPORTED" "XE_HPC_CORE" "PVC")
    if(AUBSTREAM_TESTS_PVC)
      ADD_PLATFORM_FOR_CORE_TYPE("TESTED" "XE_HPC_CORE" "PVC")
    endif()
  endif()
endif()

if(AUBSTREAM_SUPPORT_XE2_HPG_CORE)
  if(AUBSTREAM_SUPPORT_BMG)
    ADD_PLATFORM_FOR_CORE_TYPE("SUPPORTED" "XE2_HPG_CORE" "BMG")
    if(AUBSTREAM_TESTS_BMG)
      ADD_PLATFORM_FOR_CORE_TYPE("TESTED" "XE2_HPG_CORE" "BMG")
    endif()
  endif()

  if(AUBSTREAM_SUPPORT_LNL)
    ADD_PLATFORM_FOR_CORE_TYPE("SUPPORTED" "XE2_HPG_CORE" "LNL")
    if(AUBSTREAM_TESTS_LNL)
      ADD_PLATFORM_FOR_CORE_TYPE("TESTED" "XE2_HPG_CORE" "LNL")
    endif()
  endif()
endif()

if(AUBSTREAM_SUPPORT_XE3_CORE)
  if(AUBSTREAM_SUPPORT_PTL)
    ADD_PLATFORM_FOR_CORE_TYPE("SUPPORTED" "XE3_CORE" "PTL")
    if(AUBSTREAM_TESTS_PTL)
      ADD_PLATFORM_FOR_CORE_TYPE("TESTED" "XE3_CORE" "PTL")
    endif()
  endif()
endif()

if(AUBSTREAM_SUPPORT_XE3P_CORE)
  if(AUBSTREAM_SUPPORT_CRI)
    ADD_PLATFORM_FOR_CORE_TYPE("SUPPORTED" "XE3P_CORE" "CRI")
    if(AUBSTREAM_TESTS_CRI)
      ADD_PLATFORM_FOR_CORE_TYPE("TESTED" "XE3P_CORE" "CRI")
    endif()
  endif()
endif()
